# VS Code settings layout

- Shared settings live in `.vscode/settings.json`.
- Local overrides: copy `.vscode/DREAM-Workspace.local.code-workspace.example` to
  `.vscode/DREAM-Workspace.local.code-workspace`, adjust paths/personal toggles,
  and open that workspace in VS Code. The local workspace stays untracked.

